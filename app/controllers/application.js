import Ember from 'ember';

export default Ember.Controller.extend({
  /*
  currentTab: null,
  availableTabs: ["overview", "assignments", "members"],

  initialize: Ember.on('init', function() {
    set(this, 'currentTab', 'overview');
  }),

  overviewActive: Ember.computed('currentTab', {
    get: function() {
      return get(this, 'currentTab') === "overview";
    }
  }),

  assignmentsActive: Ember.computed('currentTab', {
    get: function() {
      return get(this, 'currentTab') === "assignments";
    }
  }),

  membersActive: Ember.computed('currentTab', {
    get: function() {
      return get(this, 'currentTab') === "members";
    }
  }),


  actions: {
    select: function(tabname) {
      if (get(this, 'availableTabs').indexOf(tabname) >= 0) {
        set(this, 'currentTab', tabname);
      }
    },
  }
  */
});
