<div id="ships">
  <div class="ship-list">
    {{#each model as |member|}}
      {{#if member.ships.length}}
        <div class="ships">
          <span class="ship-owner">
  {{#if member.avatar}}
    <img class="member-avatar" src={{concat-string "https://robertsspaceindustries.com" member.avatar}}>
  {{else}}
    <img class="member-avatar" src="https://robertsspaceindustries.com/rsi/static/images/account/avatar_default.jpg">
  {{/if}}

            {{member.name}} [{{member.ships.length}}]
          </span>
          <div class="col-md-12 member-ships">
            {{#each member.ships as |ship|}}
              {{#unless ship.hidden}}
              <div class="col-md-2 shipfix">
                {{ship-model-details ship=ship.model name=ship.name}}
              </div>
              {{/unless}}
            {{/each}}
          </div>
          <div class="clearfix" style="clear: both;"></div>
        </div>
      {{/if}}
    {{/each}}

    <div class="col-md-12">
      count: {{ships.length}}
    </div>
  </div>
</div>


